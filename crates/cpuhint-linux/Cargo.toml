[package]
name = "cpuhint-linux"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
cpuhint-core = { package = "cpuhint-core", path = "../cpuhint-core" }
rustix = { version = "1.1", default-features = false, features = ["fs"] }
